{
  "name": "jetton-wallet-commission",
  "version": "1.0.0",
  "description": "TON Jetton Wallet with 6% commission and 1/66 lottery",
  "main": "index.js",
  "scripts": {
    "test": "mocha --require ts-node/register **/*.spec.ts **/*-test.ts",
    "build": "cd ft && bash compile.sh",
    "deploy": "npx blueprint run deployJettonMinter",
    "validate-env": "node -e \"require('./scripts/envConfig').loadEnvConfig(); console.log('✅ Environment configuration valid');\""
  },
  "dependencies": {
    "@ton/core": "^0.56.0",
    "@ton/blueprint": "^0.20.0",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0",
    "mocha": "^10.0.0",
    "chai": "^4.3.0"
  },
  "keywords": ["ton", "jetton", "commission", "lottery", "blockchain"],
  "author": "TON Developer",
  "license": "Apache-2.0"
}
